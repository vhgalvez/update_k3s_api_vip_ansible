---
- name: "Actualizar K3s para usar la IP VIP"
  hosts: all
  become: true
  gather_facts: false

  vars:
    vip_ip: "{{ api_vip | default('10.17.5.10') }}"
    kubeconfig_path: "/etc/rancher/k3s/k3s.yaml"
    k3s_service_path: "/etc/systemd/system/k3s.service"
    agent_service_path: "/etc/systemd/system/k3s-agent.service"
    primer_master: "{{ groups['masters'][0] }}"

  tasks:

    - name: "📌 Verificar si es el primer master"
      debug:
        msg: "Soy el primer master"
      when: inventory_hostname == primer_master

    #################################################################
    #                         FASE 1 - MASTER 1                    #
    #################################################################

    - name: "➕ Añadir --tls-san VIP si no está presente (primer master)"
      raw: |
        grep -q -- "--tls-san {{ vip_ip }}" {{ k3s_service_path }} || \
        sed -i "/ExecStart=/ s|\\(--tls-san [^ ]\\+\\)|\\1 --tls-san {{ vip_ip }}|" {{ k3s_service_path }}
      when: inventory_hostname == primer_master

    - name: "❌ Eliminar --advertise-address (si existe)"
      raw: |
        sed -i "/ExecStart=/ s| --advertise-address [^ ]*||" {{ k3s_service_path }}
      when: inventory_hostname == primer_master

    - name: "🧹 Borrar certificados TLS (regeneración)"
      raw: |
        rm -rf /var/lib/rancher/k3s/server/tls
        rm -rf /var/lib/rancher/k3s/server/db/etcd-tmp
      when: inventory_hostname == primer_master

    - name: "♻️ Reiniciar K3s en primer master"
      raw: |
        systemctl daemon-reexec
        systemctl daemon-reload
        systemctl restart k3s
      when: inventory_hostname == primer_master

    - name: "⏳ Esperar que API responda por VIP (primer master)"
      raw: |
        for i in $(seq 1 60); do
          curl -sk https://{{ vip_ip }}:6443/version >/dev/null && exit 0
          sleep 1
        done
        echo "❌ La API en {{ vip_ip }}:6443 no respondió en 60s"
      when: inventory_hostname == primer_master

    #################################################################
    #                  FASE 2 - AGENTES (workers, storage)         #
    #################################################################

    - name: "📝 Reemplazar IP del API server en k3s-agent.service"
      raw: |
        sed -i "s|--server https://[^ ]\\+|--server https://{{ vip_ip }}:6443|" {{ agent_service_path }}
      when: inventory_hostname not in groups['masters']

    - name: "♻️ Reiniciar k3s-agent"
      raw: |
        systemctl daemon-reexec
        systemctl daemon-reload
        systemctl restart k3s-agent
      when: inventory_hostname not in groups['masters']

    - name: "📡 Verificar que k3s-agent está activo"
      raw: |
        systemctl is-active k3s-agent || echo "❌ El agente k3s no está activo"
      when: inventory_hostname not in groups['masters']

    #################################################################
    #                    FASE 3 - Otros masters                     #
    #################################################################

    - name: "➕ Añadir --tls-san en los demás masters"
      raw: |
        grep -q -- "--tls-san {{ vip_ip }}" {{ k3s_service_path }} || \
        sed -i "/ExecStart=/ s|\\(--tls-san [^ ]\\+\\)|\\1 --tls-san {{ vip_ip }}|" {{ k3s_service_path }}
      when: inventory_hostname in groups['masters'] and inventory_hostname != primer_master

    - name: "🧹 Borrar TLS para regeneración (otros masters)"
      raw: |
        rm -rf /var/lib/rancher/k3s/server/tls
        rm -rf /var/lib/rancher/k3s/server/db/etcd-tmp
      when: inventory_hostname in groups['masters'] and inventory_hostname != primer_master

    - name: "♻️ Reiniciar K3s en otros masters"
      raw: |
        systemctl daemon-reexec
        systemctl daemon-reload
        systemctl restart k3s
      when: inventory_hostname in groups['masters'] and inventory_hostname != primer_master

    #################################################################
    #                 FASE FINAL - Verificación                    #
    #################################################################

    - name: "🔍 Verificar kubeconfig usa VIP (si existe)"
      raw: |
        if [ -f "{{ kubeconfig_path }}" ]; then
          grep 'server: https://{{ vip_ip }}:6443' "{{ kubeconfig_path }}" || echo "❌ kubeconfig sin VIP"
        else
          echo "⚠️ No existe kubeconfig (normal en agentes)"
        fi
      register: resultado_kubeconfig
      changed_when: false

    - name: "📤 Mostrar resultado de verificación"
      debug:
        var: resultado_kubeconfig.stdout_lines